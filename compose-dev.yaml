version: '3.1'

services:

  app00:
    image: gitpod/workspace-full
    stdin_open: true

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
